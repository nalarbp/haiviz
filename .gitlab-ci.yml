#This file is for continuous integration and deployment (CI/CD) of HAIviz in FordeLab GitLab.
stages:
  - deploy

deploy-job:
  stage: deploy
  environment: production
  script:
    - rsync -avhPL --delete build_production/ /var/www/haiviz-v04/build_production/
    - echo "Application successfully deployed."
